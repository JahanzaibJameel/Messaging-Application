# Server image: Express API + WebSocket server
FROM node:20-alpine

WORKDIR /app

# Install dependencies first for better layer caching
COPY package.json package-lock.json ./
RUN npm ci --ignore-scripts

# Copy application source
COPY server ./server
COPY shared ./shared
COPY app.json ./

EXPOSE 5000

CMD ["npm", "run", "server:start"]
